El archivo "connection_datacenter.js" es el archivo que se coloca dentro del apartado "connections" de
ditto para permitir los mensajes entrantes por MQTT por parte de los nodos.

El archivo "datacener_twin" es el que contiene la estructura del thing del centro de datos. Para añadirla
es necesario usar la REST api de ditto.
